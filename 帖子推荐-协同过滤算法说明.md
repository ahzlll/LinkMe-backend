# 帖子推荐：基于用户的协同过滤算法说明（User-Based CF）

本文档描述当前后端“帖子推荐”所使用的协同过滤算法实现，便于维护、对外说明与后续优化。

## 1. 代码位置（实现入口）

- **核心实现**：`src/main/java/com/linkme/backend/service/impl/PostRecommendServiceImpl.java`
- **点赞数据来源**：`src/main/java/com/linkme/backend/mapper/LikeMapper.java`（`selectAll()`）

对外暴露的服务方法为：

- `PostRecommendServiceImpl#getRecommendedPosts(Integer userId, Integer limit)`

## 2. 使用的数据与建模方式

算法使用“**点赞（Like）**”作为隐式反馈（Implicit Feedback）信号，将用户行为抽象为集合：

- 对每个用户 \(u\)，定义点赞集合 \(L_u = \{\text{postId} \mid u \text{ 点赞过该帖子}\}\)

实现中会一次性拉取所有点赞记录并构建映射：

- `userPostMap: Map<userId, Set<postId>>`

## 3. 总体流程（端到端）

### 3.1 兜底与冷启动处理

以下情况会直接走兜底策略（返回“热门/最新”帖子）：

- `userId == null`：未登录用户
- 系统内无点赞记录：`LikeMapper.selectAll()` 为空
- 当前用户无点赞：`L_u` 为空（无法计算相似度）
- 找不到任何相似用户：相似度表为空
- 最终候选帖子为空：无法产出推荐

对应实现：`getPopularPosts(userId, limit)`（当前用 `postMapper.selectAll(0, limit, userId)` 近似热门/最新）

### 3.2 计算用户相似度（Jaccard Similarity）

对每个其他用户 \(v \ne u\)，计算：

\[
sim(u,v) = \frac{|L_u \cap L_v|}{|L_u \cup L_v|}
\]

实现细节：

- 只保留 `sim(u,v) > 0` 的用户（即至少有一个共同点赞帖子）
- 相似度计算函数为 `calculateJaccardSimilarity(Set<Integer> set1, Set<Integer> set2)`

### 3.3 候选帖子生成与打分（相似度加权求和）

从所有相似用户的点赞集合中抽取候选帖子：

- 若帖子 \(p \in L_v\) 且 \(p \notin L_u\)，则视为候选

对候选帖子按相似度累加分数：

\[
score(p) = \sum_{v \in S(u)} sim(u,v) \cdot \mathbb{I}[p \in L_v \land p \notin L_u]
\]

其中 \(S(u)\) 为与 \(u\) 相似的用户集合，\(\mathbb{I}[\cdot]\) 为指示函数。

实现对应：

- `postScoreMap: Map<postId, Double>`
- 累加逻辑：`postScoreMap.put(postId, postScoreMap.getOrDefault(postId, 0.0) + similarity)`

### 3.4 排序与截断（Top-N）

- 将 `postScoreMap` 按分数降序排序
- 取前 `limit` 个 `postId`
- 再通过 `postMapper.selectByIds(recommendedPostIds, userId)` 查询帖子详情返回

### 3.5 数量不足时补齐（热门/最新补齐 + 去重）

当协同过滤返回数量不足 `limit` 时：

- 再取一批热门/最新帖子
- 对已推荐帖子做去重
- 依次补齐到 `limit`

## 4. 算法特点与适用范围

- **优点**
  - 实现简单、可解释：共同点赞越多越相似，越相似的人点赞过的帖子越值得推荐
  - 对内容特征依赖小：无需帖子标签/文本向量即可工作
- **局限**
  - **冷启动问题**：新用户无点赞、或总体点赞稀疏时会大量触发兜底
  - **稀疏与扩展性**：当前实现一次性 `selectAll()` 拉取全部点赞，数据量上升后会带来性能压力
  - **热门偏置**：共同点赞更容易发生在热门帖上，可能导致推荐趋同

## 5. 复杂度与性能（当前实现）

记：

- \(U\)：有点赞记录的用户数
- \(M\)：点赞记录总数（Like 行数）
- \(|L_u|\)：目标用户点赞数

当前实现的主要代价：

- 构建 `userPostMap`：\(O(M)\)
- 计算相似度（对所有其他用户遍历 + 交集计算）：近似 \(O\left(\sum_{v\ne u} \min(|L_u|, |L_v|)\right)\)
- 生成候选并累加分：\(O\left(\sum_{v \in S(u)} |L_v|\right)\)

当点赞数据变大时，瓶颈通常来自：

- 全量拉取点赞 `selectAll()`
- 对所有用户做相似度遍历

## 6. 可选优化方向（按优先级）

- **限制相似用户 Top-K**：只取相似度最高的 K 个用户参与打分（减少候选遍历与热门噪声）
- **时间衰减**：对点赞行为按时间衰减（近期点赞权重更高）
- **改进相似度**：Jaccard 可替换为 Cosine（基于二值向量）、或对热门帖做惩罚（如 IDF 思路）
- **离线/缓存**：
  - 定期离线计算用户相似度或用户推荐列表
  - 将 `userPostMap`/相似用户列表缓存（Redis/本地缓存）
- **数据层优化**：
  - 将 `selectAll()` 改为按需查询（例如只拉取“当前用户点赞过的帖子”的点赞用户集合，再扩展到相似用户）

## 7. 当前兜底策略说明

`getPopularPosts(userId, limit)` 当前实现采用 `postMapper.selectAll(0, limit, userId)` 作为“热门/最新”近似：

- 优点：简单可靠，确保总有结果
- 风险：严格意义上不是真正“按点赞数热门”，可能更偏“最新”

如需真正热门，可在数据层增加：

- 按点赞数聚合的查询
- 或维护“热门帖子榜单”缓存表/定时任务

