算法概览

- 入口在匹配推荐接口，按分数降序分页返回候选人列表 MatchController.java
- 核心计算在匹配服务实现，基于“基础分 + 奖惩项”，并对 must/优先维度进行约束调节 MatchRecommendServiceImpl.java




打分构成

- 基础分 80 MatchRecommendServiceImpl.java
- 年龄不在当前用户的交友年龄范围内：-10 calculateScore
- 距离偏好为 same_city 且不在同地区：-5 calculateScore
- 爱好重合：同类爱好 +1，运动户外类（category_id=3）+2 calculateScore
   重点爱好可同理设置
   
- 关系品质交集：每项 +2，最多 +6 applyPersonalityAndPreferenceScore
- 关系模式一致：+5 applyPersonalityAndPreferenceScore
- 沟通期待一致：+3 applyPersonalityAndPreferenceScore
- 性格特质匹配：
  - self-self 同类目值相同：每项 +1 applyPersonalityAndPreferenceScore
  - ideal-self 映射加分：每命中项 +2（社交风格、处事风格两类映射） calculateIdealToSelfScore
Must/优先维度调节

- must 维度不满足：每项 -10 applyPersonalityAndPreferenceScore
  - age_range：双方年龄偏好互相满足，否则扣分 isAgeSatisfiedMutual
  - distance：双方距离偏好互相满足，否则扣分 isDistanceSatisfiedMutual
  - interest_overlap：无重合兴趣扣分
  - personality_match：性格匹配分数为 0 扣分
  - relationship_mode/communication_style：不一致扣分
- 优先维度小加权：最多 3 项，权重 3/2/1 applyPersonalityAndPreferenceScore
  - 满足对应维度条件时按权重加分（如年龄互相满足、同城/远程满足、兴趣有重合、性格有得分、关系模式一致、沟通风格一致）
- 维度定义的 code 来自初始化数据：age_range、distance、interest_overlap、personality_match、relationship_mode、communication_style test.sql
- must/priority 的 code 来源查询： UserMatchingDimensionMapper.java
候选人筛选与排序

- 候选人批量拉取：按当前用户过滤后取 N 条（默认 size*5 至少 100） getRecommendations
- 批量预取候选人的偏好、性格、关系品质、兴趣用于打分 getRecommendations
- 逐个计算得分后，按得分降序、userId 次序排序，分页返回 getRecommendations
- 分数最终被限制在 0–100 区间 clamp
接口与数据来源

- 获取推荐列表：GET /match/recommendations，需携带 Bearer Token MatchController.java
- 用户偏好/性格/关系品质/兴趣等均来自各表及其 Mapper 批量查询：
  - 用户偏好 UserMatchingPreferenceMapper.xml
  - 性格选择（含分类名与选项名） UserPersonalityMapper.xml
  - 关系品质选择 UserRelationshipQualityMapper.xml
  - 兴趣列表 UserHobbyMapper.xml
  - must/priority 维度 UserMatchingDimensionMapper.xml
如果你想调整“匹配1”的权重或规则（比如提高性格映射权重，或将 must 改为硬过滤不计分），告诉我你的方向，我可以直接在上述位置替换实现并验证结果
