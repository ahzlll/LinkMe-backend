# LinkMe 管理端使用说明

## 1. 数据库迁移（首次必做）

在 MySQL 客户端（Navicat / Workbench / 命令行）对 `linkme` 库执行：

**完整迁移：**

```bash
mysql -u root -p linkme < src/main/resources/sql/migration_admin_module.sql
```

**若只报 `Unknown column 'moderation_status'`**，执行：

```sql
USE linkme;
ALTER TABLE post ADD COLUMN moderation_status VARCHAR(16) NOT NULL DEFAULT 'visible' COMMENT 'visible|hidden|deleted' AFTER topic;
ALTER TABLE comment ADD COLUMN moderation_status VARCHAR(16) NOT NULL DEFAULT 'visible' COMMENT 'visible|hidden|deleted' AFTER content;
```

或运行脚本：`src/main/resources/sql/migration_moderation_columns.sql`

新增字段/表：
- `user.account_status`、`ban_until`、`status_reason`（账号处罚，与 `role` 权限分离）
- `post.moderation_status`、`comment.moderation_status`（visible / hidden / deleted）
- `admin_operation_log`（管理员操作日志）
- 复用已有 `audit_log`（内容审核日志）

## 2. 管理员账号

测试库默认管理员（见 `test.sql`）：
- 账号：`admin1` 或邮箱 `admin@linkme.com`
- 密码：与其它测试用户相同（文档中为 `Aa123456_` 对应哈希，以你本地库为准）
- `role` 需为 `admin` 或 `moderator`

## 3. 登录与入口

1. 打开前端 `/login`
2. 选择 **管理员登录**
3. 登录成功后进入 `/admin`（普通用户选「用户登录」进发现页）

路由守卫：`/admin` 仅 `admin` / `moderator` 可访问。

## 4. 功能概览

| 模块 | 能力 |
|------|------|
| 用户管理 | 搜索、按角色/状态筛选；查看 ID、昵称、账号、头像、性别、地区、注册时间、角色、账号状态 |
| 账号处罚 | 警告、限制发帖、限制评论、临时封禁（可设天数）、永久封禁、解封 |
| 帖子/评论 | 隐藏、审核通过、驳回、删除 |
| 日志 | `audit_log` 内容审核记录；`admin_operation_log` 封禁/解封/删除等管理操作 |

## 5. 主要 API（需 Bearer Token + 管理员角色）

- `GET /admin/users` — 用户列表（`keyword`、`role`、`accountStatus`）
- `POST /admin/users/{id}/punish` — 处罚 `{ "action": "warn|restricted_post|...", "reason": "...", "banDays": 7 }`
- `POST /admin/users/{id}/unban` — 解封
- `POST /admin/posts/{id}/moderate` — `{ "action": "hide|approve|reject|delete", "reason": "..." }`
- `POST /admin/comments/{id}/moderate` — 同上
- `GET /admin/audit-logs`、`GET /admin/operation-logs` — 日志查询

## 6. 设计说明

- **`role`**：身份权限（customer / admin / moderator），决定能否进管理端。
- **`account_status`**：账号治理状态（封禁、限发帖等），不再用 `role=banned`（避免破坏 ENUM）。
- 被封禁用户无法用「用户登录」进入；管理员入口会校验管理员角色。

## 7. 匹配问卷与冷启动推荐

### 问卷完成状态

用户表 `user` 中的 `matching_questionnaire_completed` 字段标记用户是否已完成匹配问卷：

- **FALSE**（默认）：用户未完成问卷，处于冷启动状态
  - 可浏览推荐列表（基于同城 + 年龄接近度评分）
  - **不能**发起喜欢（红心）操作 — 后端返回 403
  - **不能**发起聊天 — 后端返回 403
- **TRUE**：用户已完成问卷
  - 推荐列表加入问卷加分（兴趣重叠、性格匹配等）
  - 可正常使用喜欢和聊天功能

### 问卷提交机制

- **POST** 请求（最终提交）：标记 `matching_questionnaire_completed = TRUE`
- **PUT** 请求（自动保存草稿）：**不**标记完成

### 精简后的问卷字段（7 步）

1. 兴趣爱好（多选）
2. 社交能量来源（extroverted / introverted / ambivert）
3. 决策方式（rational / emotional / balanced）
4. 生活节奏（planned / spontaneous / flexible）
5. 年龄要求（min/max/unlimited）
6. 关系距离（same_city / same_city_or_remote / unlimited）
7. 额外要求（选填文本）

> 已移除的字段：沟通风格、理想社交/生活/兴趣特质、关系品质、关系模式、沟通期待、必须/优先维度。这些字段对应的表结构仍保留在数据库中，但不再被应用代码读写。
