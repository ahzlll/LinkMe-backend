<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linkme.backend.mapper.LikeMapper">

    <resultMap id="LikeMap" type="com.linkme.backend.entity.Like">
        <id column="like_id" property="likeId"/>
        <result column="user_id" property="userId"/>
        <result column="post_id" property="postId"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <select id="selectByUserAndPost" resultMap="LikeMap">
        SELECT * FROM like_post WHERE user_id = #{userId} AND post_id = #{postId}
    </select>

    <select id="selectByPostId" resultMap="LikeMap">
        SELECT * FROM like_post WHERE post_id = #{postId}
        ORDER BY created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="selectByUserId" resultMap="LikeMap">
        SELECT * FROM like_post WHERE user_id = #{userId}
        ORDER BY created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insert" parameterType="com.linkme.backend.entity.Like" useGeneratedKeys="true" keyProperty="likeId">
        INSERT INTO like_post (user_id, post_id, created_at)
        VALUES (#{userId}, #{postId}, #{createdAt})
    </insert>

    <delete id="deleteByUserAndPost">
        DELETE FROM like_post WHERE user_id = #{userId} AND post_id = #{postId}
    </delete>

    <select id="countByPostId" parameterType="int" resultType="int">
        SELECT COUNT(*) FROM like_post WHERE post_id = #{postId}
    </select>

    <select id="countByUserId" parameterType="int" resultType="int">
        SELECT COUNT(*) FROM like_post WHERE user_id = #{userId}
    </select>

</mapper>


