<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linkme.backend.mapper.ManualReviewQueueMapper">

    <resultMap id="ManualReviewQueueMap" type="com.linkme.backend.entity.ManualReviewQueue">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="content_type" property="contentType"/>
        <result column="content_id" property="contentId"/>
        <result column="content" property="content"/>
        <result column="matched_words" property="matchedWords"/>
        <result column="categories" property="categories"/>
        <result column="status" property="status"/>
        <result column="reviewer_id" property="reviewerId"/>
        <result column="review_remark" property="reviewRemark"/>
        <result column="create_time" property="createTime"/>
        <result column="review_time" property="reviewTime"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO manual_review_queue (user_id, content_type, content_id, content,
                                        matched_words, categories, status, create_time)
        VALUES (#{userId}, #{contentType}, #{contentId}, #{content},
                #{matchedWords}, #{categories}, #{status}, #{createTime})
    </insert>

    <select id="selectById" resultMap="ManualReviewQueueMap">
        SELECT * FROM manual_review_queue WHERE id = #{id}
    </select>

    <select id="selectPending" resultMap="ManualReviewQueueMap">
        SELECT * FROM manual_review_queue
        WHERE status = 0
        ORDER BY create_time ASC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countPending" resultType="int">
        SELECT COUNT(*) FROM manual_review_queue WHERE status = 0
    </select>

    <update id="updateStatus">
        UPDATE manual_review_queue
        SET status = #{status}, reviewer_id = #{reviewerId},
            review_remark = #{reviewRemark}, review_time = NOW()
        WHERE id = #{id}
    </update>

    <select id="selectByUserId" resultMap="ManualReviewQueueMap">
        SELECT * FROM manual_review_queue
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>
</mapper>