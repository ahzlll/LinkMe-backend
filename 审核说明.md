# LinkMe 审核功能说明

## 一、功能概述

本项目集成了一套完整的内容审核系统，包含**算法一审**和**人工复审**两个核心环节，确保用户发布的内容符合平台规范。

---

## 二、审核流程设计

### 2.1 完整流程架构

```
用户发布内容
    ↓
┌─────────────────┐
│  算法一审       │  ← 敏感词自动检测
│  (自动审核)     │
└────────┬────────┘
         │
    ┌────┴────┐
    ↓         ↓
  通过      未通过
    ↓         ↓
内容发布   ┌─────────────────┐
           │  人工复审队列   │  ← 等待管理员审核
           └────────┬────────┘
                    │
           ┌────────┴────────┐
           ↓                 ↓
        审核通过          审核拒绝
           ↓                 ↓
        内容发布          内容拒绝
                             ↓
                    ┌─────────────────┐
                    │  事后下架       │  ← 已发布内容可随时下架
                    └─────────────────┘
```

### 2.2 审核状态流转

| 状态 | 说明 | 触发条件 |
|------|------|----------|
| `AUTO_PASS` | 自动通过 | 敏感词检测无匹配 |
| `NEED_MANUAL` | 待人工复审 | 检测到敏感词 |
| `MANUAL_PASS` | 人工审核通过 | 管理员批准 |
| `MANUAL_REJECT` | 人工审核拒绝 | 管理员拒绝 |
| `OFFLINE` | 事后下架 | 对已发布内容执行下架 |

---

## 三、核心实现

### 3.1 敏感词检测引擎

#### 3.1.1 主引擎：sensitive-word 开源库

**项目来源**：[houbb/sensitive-word](https://github.com/houbb/sensitive-word)

**版本**：`0.29.5`

**算法原理**：采用 **DFA（确定性有限自动机）** 算法，具有以下特点：
- 时间复杂度 O(n)，检测速度快
- 支持多种变形检测（大小写、全角半角、数字变形等）
- 内置丰富的敏感词词库

**核心配置参数**：
```java
SensitiveWordBs.newInstance()
    .ignoreCase(true)           // 忽略大小写
    .ignoreWidth(true)          // 忽略全角半角差异
    .ignoreNumStyle(true)       // 忽略数字变形（如 1314 vs 一三一四）
    .ignoreChineseStyle(true)   // 忽略中文书写格式差异
    .ignoreRepeat(true)         // 忽略重复字符
    .enableNumCheck(true)       // 启用数字检测（如电话号码）
    .enableEmailCheck(true)     // 启用邮箱检测
    .enableUrlCheck(true)       // 启用URL检测
    .init();
```

#### 3.1.2 降级方案：本地敏感词服务

**服务类**：`LocalSensitiveWordService`

**作用**：当主引擎初始化失败时自动启用，确保系统可用性

**默认敏感词示例**：
- 政治敏感类：台独、分裂国家、颠覆政权
- 暴力恐怖类：暴力恐怖、血腥暴力
- 色情低俗类：色情低俗
- 违法犯罪类：赌博诈骗、毒品

### 3.2 数据库设计

#### 3.2.1 审核日志表（audit_log）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| user_id | BIGINT | 用户ID |
| content_type | VARCHAR | 内容类型（POST/CHAT等） |
| content_id | BIGINT | 内容ID |
| content | TEXT | 审核内容（截取前500字符） |
| is_violation | TINYINT | 是否违规（0=否，1=是） |
| matched_words | VARCHAR | 匹配到的敏感词 |
| categories | VARCHAR | 违规类别 |
| audit_result | TINYINT | 审核结果 |
| reviewer_id | BIGINT | 审核员ID（人工审核时） |
| remark | VARCHAR | 审核备注 |
| create_time | DATETIME | 创建时间 |
| audit_time | DATETIME | 审核完成时间 |

#### 3.2.2 人工复审队列表（manual_review_queue）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| user_id | BIGINT | 用户ID |
| content_type | VARCHAR | 内容类型 |
| content_id | BIGINT | 内容ID |
| content | TEXT | 完整内容 |
| matched_words | VARCHAR | 匹配到的敏感词 |
| categories | VARCHAR | 违规类别 |
| status | TINYINT | 状态（0=待审核，1=通过，2=拒绝） |
| reviewer_id | BIGINT | 审核员ID |
| remark | VARCHAR | 审核备注 |
| create_time | DATETIME | 创建时间 |
| review_time | DATETIME | 审核时间 |

---

## 四、API 接口

### 4.1 管理员审核接口

#### 4.1.1 获取待复审列表

**接口地址**：`GET /api/audit/pending`

**参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| page | INT | 页码（默认1） |
| size | INT | 每页数量（默认10） |

**响应示例**：
```json
{
  "code": 200,
  "data": {
    "list": [...],
    "total": 100,
    "page": 1,
    "size": 10
  }
}
```

#### 4.1.2 审核通过

**接口地址**：`POST /api/audit/approve`

**参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| queueId | LONG | 复审队列ID |
| reviewerId | LONG | 审核员ID |
| remark | STRING | 备注（可选） |

#### 4.1.3 审核拒绝

**接口地址**：`POST /api/audit/reject`

**参数**：同上

#### 4.1.4 事后下架

**接口地址**：`POST /api/audit/offline`

**参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| contentType | STRING | 内容类型 |
| contentId | LONG | 内容ID |
| auditorId | LONG | 操作人ID |
| remark | STRING | 下架原因 |

#### 4.1.5 获取审核统计

**接口地址**：`GET /api/audit/stats`

**响应示例**：
```json
{
  "code": 200,
  "data": {
    "total": 1000,
    "autoPass": 800,
    "needManual": 150,
    "manualPass": 120,
    "manualReject": 30,
    "offline": 50,
    "pendingReview": 10
  }
}
```

#### 4.1.6 热重载词库

**接口地址**：`POST /api/audit/reload`

**说明**：重新初始化敏感词库（仅对主引擎生效）

### 4.2 内容发布前置审核

**说明**：在 PostController 和 ChatController 中集成审核逻辑

**流程**：
1. 用户提交内容
2. 调用 `AuditService.checkContent()` 进行检测
3. 根据返回结果决定是否允许发布

---

## 五、文件结构

```
src/main/java/com/linkme/backend/
├── controller/
│   └── AuditController.java          # 管理员审核接口
├── service/
│   ├── AuditService.java             # 审核服务接口
│   ├── LocalSensitiveWordService.java # 本地敏感词服务（降级）
│   └── impl/
│       └── AuditServiceImpl.java     # 审核服务实现
├── config/
│   └── LocalSensitiveWordConfig.java # 本地敏感词配置
├── entity/
│   ├── AuditLog.java                 # 审核日志实体
│   └── ManualReviewQueue.java        # 复审队列实体
├── mapper/
│   ├── AuditLogMapper.java           # 审核日志数据库操作
│   └── ManualReviewQueueMapper.java  # 复审队列数据库操作
└── resources/
    └── sql/
        └── migration_audit.sql       # 数据库初始化脚本
```

---

## 六、依赖说明

### 6.1 敏感词库依赖

**主依赖**：
```xml
<dependency>
  <groupId>com.github.houbb</groupId>
  <artifactId>sensitive-word</artifactId>
  <version>0.29.5</version>
</dependency>
```

**传递依赖**：
| 依赖 | 版本 | 说明 |
|------|------|------|
| heaven | 0.13.0 | 工具库 |
| opencc4j | 1.14.0 | 中文繁简体转换 |
| sensitive-word-data | 1.0.0 | 敏感词数据 |

### 6.2 离线部署方案

为确保项目在无网络环境或依赖仓库不可用时仍能正常运行，已将相关 JAR 文件打包至 `lib/` 目录：

```
lib/
├── sensitive-word-0.29.5.jar
├── heaven-0.13.0.jar
├── opencc4j-1.14.0.jar
└── sensitive-word-data-1.0.0.jar
```

**使用方式**：在 `pom.xml` 中配置系统范围依赖

---

## 七、异常处理与降级机制

### 7.1 多级降级策略

```
┌─────────────────────────────────────────────────────┐
│                   审核请求                          │
└──────────────────────────┬──────────────────────────┘
                           ↓
              ┌─────────────────────┐
              │  主引擎检测         │
              │  (sensitive-word)   │
              └──────────┬──────────┘
                   成功  │  失败
                      ↓  ↓
              ┌────────┴────────┐
              ↓                 ↓
         返回结果      ┌───────────────────┐
                      │  本地服务检测     │
                      │ (LocalSensitive)  │
                      └──────────┬────────┘
                           成功  │  失败
                              ↓  ↓
                         返回结果  ↓
                          ┌──────┴──────┐
                          ↓             ↓
                    记录异常       进入人工复审
                    日志           (兜底策略)
```

### 7.2 异常场景处理

| 异常场景 | 处理方式 | 结果 |
|----------|----------|------|
| 敏感词库初始化失败 | 自动切换到本地服务 | 继续运行 |
| 检测过程抛出异常 | 记录日志，使用本地服务重试 | 继续运行 |
| 本地服务也不可用 | 内容进入人工复审队列 | 不阻塞发布流程 |
| 数据库操作失败 | 记录日志，不影响审核流程 | 审核结果可能丢失 |

---

## 八、安全与合规

### 8.1 数据安全

- 审核日志保留完整的审核记录，便于追溯
- 敏感内容在日志中进行长度限制（500字符）
- 数据库操作使用参数化查询，防止 SQL 注入

### 8.2 审核合规

- 支持多种违规类型的分类记录
- 人工审核记录审核员信息，便于责任追溯
- 事后下架功能支持对已发布内容进行处理

---

## 九、扩展建议

### 9.1 可优化点

1. **词库管理界面**：提供敏感词库的增删改查功能
2. **自定义规则**：支持管理员自定义审核规则
3. **机器学习集成**：引入 AI 模型进行更精准的内容分析
4. **实时监控**：添加审核流量和结果的实时监控
5. **批量处理**：支持批量审核操作

### 9.2 性能优化

1. **词库缓存**：将敏感词库缓存到 Redis，减少内存占用
2. **异步审核**：对非实时场景使用异步审核
3. **分布式部署**：支持审核服务的水平扩展

---

## 十、测试用敏感词示例

### 10.1 测试说明

为方便测试审核功能，以下列出各类敏感词示例，可用于发布测试内容：

### 10.2 敏感词分类示例

| 分类 | 敏感词示例 | 预期结果 |
|------|----------|----------|
| **政治敏感** | 台独、分裂国家、颠覆政权、反动言论 | 触发审核 |
| **暴力恐怖** | 暴力恐怖、血腥暴力、枪支、爆炸物、核弹 | 触发审核 |
| **色情低俗** | 色情、色情低俗、裸、色情网站、一夜情、约炮 | 触发审核 |
| **赌博诈骗** | 赌博诈骗、赌场、博彩、电信诈骗、网络诈骗 | 触发审核 |
| **毒品相关** | 毒品、制毒、贩卖毒品 | 触发审核 |
| **违法犯罪** | 假币、腐败、贪污、造假、黑客、入侵、木马、病毒 | 触发审核 |
| **联系方式** | 微信号、QQ号、手机号、私人联系方式 | 触发审核 |

### 10.3 测试内容示例

**示例1：包含政治敏感词**
```
今天讨论一下台独问题，分裂国家是不对的。
```

**示例2：包含暴力恐怖词**
```
这里有枪支和爆炸物出售，价格便宜。
```

**示例3：包含色情低俗词**
```
提供色情服务，一夜情约炮加我微信号。
```

**示例4：包含赌博诈骗词**
```
网络赌博平台，充值返利，加QQ号联系。
```

**示例5：包含毒品相关词**
```
出售各种毒品，量大从优，联系电话138xxxxxxxx。
```

**示例6：正常内容（应通过审核）**
```
今天天气真好，出去散步吧！
```

### 10.4 测试步骤

1. 启动后端服务
2. 使用帖子发布接口或消息发送接口提交包含敏感词的内容
3. 检查响应是否正确（应返回审核未通过或进入复审队列）
4. 通过管理员接口查看待复审列表
5. 执行人工审核操作（通过/拒绝）
6. 验证审核结果是否正确记录到数据库
